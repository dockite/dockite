import { registerField } from '@dockite/field';

import { FIELD_TYPE } from '../types';

import { SettingsComponent } from './Settings';

export default registerField(FIELD_TYPE, null, SettingsComponent);
