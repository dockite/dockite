mutation ResetUserPassword($email: String!) {
  resetUserPassword(email: $email) {
    id
    email
    firstName
    lastName
    roles {
      name
    }
    scopes
    normalizedScopes
    createdAt
    updatedAt
  }
}
