/**
 * A wrapper around the Error class to provide a more meaningful
 * exception when an issue is encountered with the Dockite Configuration.
 */
export class ConfigurationError extends Error {}

export default ConfigurationError;
