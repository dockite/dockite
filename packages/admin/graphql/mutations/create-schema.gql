mutation CreateSchema($name: String!, $title: String!, $type: SchemaType!, $groups: JSON!, $settings: JSON!) {
  createSchema(name: $name, title: $title, type: $type, groups: $groups, settings: $settings) {
    id
    name
    type
    groups
    settings
    userId
    createdAt
    updatedAt
  }
}
