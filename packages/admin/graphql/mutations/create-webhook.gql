mutation CreateWebhook($name: String!, $url: String!, $options: JSON!, $method: String!) {
  createWebhook(name: $name, url: $url, options: $options, method: $method) {
    id
    name
    url
    method
    options
    createdAt
    updatedAt
  }
}
