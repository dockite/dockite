/**
 * A wrapper around the Error class to provide a more meaningful
 * exception when an issue is encountered with field registration.
 */
export class FieldRegistrationError extends Error {}

export default FieldRegistrationError;
