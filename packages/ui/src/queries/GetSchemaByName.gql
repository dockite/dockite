query GetSchemaByName($name: String!) {
  getSchema(name: $name) {
    id
    name
    type
    groups
    settings
    fields {
      id
      name
      title
      description
      type
      settings
    }
  }
}
