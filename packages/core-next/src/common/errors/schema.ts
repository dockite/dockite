/**
 * A wrapper around the Error class to provide a more meaningful
 * exception when an issue is encountered with Schema Generation.
 */
export class SchemaGenerationError extends Error {}

export default SchemaGenerationError;
